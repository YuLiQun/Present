<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="qunzai.present">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:name=".single.MyApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mian.WelcomeActivity"
            android:theme="@style/CustomTitle">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mian.MainActivity"
            android:theme="@style/CustomTitle">

        </activity>
        <activity android:name=".mian.login.LoginActivity"
            android:theme="@style/CustomTitle">

        </activity>
        <activity android:name=".mian.settimgs.SettingsActivity"
            android:theme="@style/CustomTitle"
            >

        </activity>
    </application>

</manifest>