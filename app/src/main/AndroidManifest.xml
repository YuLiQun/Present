<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="qunzai.present">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:name=".internet.MyApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.WelcomeActivity"
            android:theme="@style/CustomTitle">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity"
            android:theme="@style/CustomTitle">

        </activity>
        <activity android:name=".main.login.LoginActivity"
            android:theme="@style/CustomTitle">

        </activity>
        <activity android:name=".main.settimgs.SettingsActivity"
            android:theme="@style/CustomTitle"
            >

        </activity>
        <activity android:name=".main.find.FindActivity"
            android:theme="@style/CustomTitle">

        </activity>

        <activity android:name=".home.homerepeat.homedetails.HomeDetailsActivity"
            android:theme="@style/CustomTitle"
            >
        </activity>

        <activity android:name=".hotsale.repeat.hotdetails.HotDetailsActivity"
            android:theme="@style/CustomTitle"
            >

        </activity>
    </application>

</manifest>